# # vault role to create certificates
# # to enable creation of CA certificates - the role must include Cert sign and CRL sign key usage options!
# apiVersion: vault.vault.upbound.io/v1alpha1
# kind:
