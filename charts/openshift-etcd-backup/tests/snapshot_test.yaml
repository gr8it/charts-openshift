suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: openshift-etcd-hub01-backup
  namespace: apc-backup

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
