suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.com
release:
  name: application-namespace-config

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
