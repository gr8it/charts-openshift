# openshift-virtualization-config

This chart renders the `HyperConverged` configuration for OpenShift Virtualization.

`values.example.yaml` mirrors the existing `conf-socpoist/ocp-dev01/ocp-virt` `HyperConverged` deployment shape so rendered examples stay close to the real GitOps usage.
