# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# VLAN network for VM live migration network
vmlmInterface:
  baseIface: ""
  vlanId: ""
  nodeSelector: {}
  whereabouts:
    rangeCidr: "" # CIDR range for VM live migration network
    rangeStart: "" # start of the IP range for VM live migration network
    rangeEnd: "" # end of the IP range for VM live migration network

# VLAN networks for VMs
vmInterfaces: []
# - baseIface: ""
#   vlanId: ""
#   nodeSelector: {}
