suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: openshift-virtualization-config
  namespace: openshift-cnv

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
