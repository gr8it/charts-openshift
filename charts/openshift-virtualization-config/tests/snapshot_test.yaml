suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: openshift-virtualization-config

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
