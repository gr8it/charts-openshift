apiVersion: v2
name: acm-policy
description: A Helm chart to install ACM policy using ACM policy framework, i.e. on hub cluster with placement decisions = deployed to selected managed clusters
type: application
version: 1.0.0
appVersion: "v1"
