suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.com
release:
  name: resource-quota-config

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
