suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: acm-operatorpolicy
  namespace: operator-namespace

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
