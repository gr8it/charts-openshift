apiVersion: v1
kind: Secret
metadata:
  name: {{ include "keycloak.fullname" . }}-ca-certs
  labels:
  {{- include "keycloak.labels" . | nindent 4 }}
stringData:
  RootCA-Sp2.crt: {{ required "caCerts.rootCASp2Crt is required" .Values.caCerts.rootCASp2Crt
    | quote }}
type: Opaque
