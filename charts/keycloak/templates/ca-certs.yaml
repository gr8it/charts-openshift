apiVersion: v1
kind: Secret
metadata:
  name: {{ include "keycloak.fullname" . }}-ca-certs
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "keycloak.labels" . | nindent 4 }}
stringData:
  {{- .Values.caCertificates | default .Values.global.apc.caCertificates| nindent 2 }}
type: Opaque
