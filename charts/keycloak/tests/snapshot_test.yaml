suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: keycloak
  namespace: apc-keycloak

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
