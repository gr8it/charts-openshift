apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.metallbns }}
  labels:
    openshift.io/cluster-monitoring: "{{ .Values.nsMonitoring }}"
    {{- include "metallb-config.labels" . | nindent 4 }}
spec: {}