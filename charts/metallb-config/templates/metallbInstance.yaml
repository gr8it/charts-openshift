apiVersion: metallb.io/v1beta1
kind: MetalLB
metadata:
  name: metallb
  namespace: {{ .Values.metallbns}}
spec:
  controllerConfig:
    priorityClassName: high-priority
  speakerConfig:
    priorityClassName: high-priority