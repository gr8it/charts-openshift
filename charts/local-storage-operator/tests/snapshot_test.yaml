suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: local-storage-operator
  namespace: openshift-local-storage

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
