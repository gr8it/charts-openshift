suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: cert-utils-operator
  namespace: cert-utils-operator

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
