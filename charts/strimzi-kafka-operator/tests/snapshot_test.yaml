suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: strimzi-kafka-operator
  namespace: openshift-operators

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
