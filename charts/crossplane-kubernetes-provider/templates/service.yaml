apiVersion: v1
kind: Service
metadata:
  name: {{ include "crossplane-kubernetes-provider.fullname" . }}
  labels:
    {{- include "crossplane-kubernetes-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crossplane-kubernetes-provider.selectorLabels" . | nindent 4 }}
