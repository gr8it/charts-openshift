suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.lint.yaml
release:
  name: vector
  namespace: apc-logging
tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
