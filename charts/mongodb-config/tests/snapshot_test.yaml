suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: eds-mongodb
  namespace: eds

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
