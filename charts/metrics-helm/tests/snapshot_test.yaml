suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: metrics-helm
  namespace: openshift-monitoring

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
