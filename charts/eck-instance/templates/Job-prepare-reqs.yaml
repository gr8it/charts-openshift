apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-prepare-reqs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "eck-instance.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      backoffLimit: 0
      containers:
