kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-monitor-role
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "eck-instance.labels" $ | nindent 4 }}
stringData:
  roles.yml: |-
    monitor_role:
      cluster:
        - "monitor"
