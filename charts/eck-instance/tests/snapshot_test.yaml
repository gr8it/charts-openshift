suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: eck-instance
  namespace: eds

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
