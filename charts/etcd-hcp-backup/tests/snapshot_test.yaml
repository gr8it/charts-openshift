suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: etcd-hcp-dev01-backup
  namespace: apc-backup

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
