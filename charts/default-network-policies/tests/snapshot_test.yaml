suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: default-network-policies

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
