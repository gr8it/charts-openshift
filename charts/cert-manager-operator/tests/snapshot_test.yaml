suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: cert-manager-operator
  namespace: apc-cert-manager-operator

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
