{{- if .Values.adIntegration.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ad-ldap-bind-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cluster-instance-hcp.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ include "apc-global-overrides.require-ESODefaultClusterSecretStore" . }}
    kind: ClusterSecretStore
  target:
    name: ad-ldap-bind-secret
    creationPolicy: Owner
  data:
    - secretKey: bindPassword
      remoteRef:
        key: {{ include "apc-global-overrides.require-vaultKVmountPlatform" . }}/{{ include "apc-global-overrides.require-environmentShort" . }}/{{ required "adIntegration.vaultKey is required when adIntegration.enabled" .Values.adIntegration.vaultKey }}
        property: {{ .Values.adIntegration.vaultProperty }}
{{- end }}
