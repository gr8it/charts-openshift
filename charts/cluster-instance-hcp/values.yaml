nameOverride: ""
fullnameOverride: ""

## uses following global values => do not set here
# global:
#   apc:
#     proxy: ~
#     noProxy: ~
#     ntpServers: []
#     caCertificates: {}
#     sshAuthorizedKeys: ~
#     cluster:
#       rootDomain: ~
#     imageProxy:
#       host: ~
#       sources: []
#     adIntegration: {}
#     ldapIntegration: {}
#     services:
#       externalSecretsOperator:
#         defaultClusterSecretStore: ~
#       vault:
#         KVmountPlatform: ~
#       certManager:
#         defaultClusterIssuer: ~

# HCP cluster name — also used as: namespace, infraID, ManagedCluster name
clusterName: ""

# ACM cluster set label; defaults to clusterName with trailing numbers removed (e.g. prod01 => prod)
clusterSet: ""

# Infrastructure namespace — defaults to infrastructure-<clusterName>
infrastructureNamespace: ""

# OCP release image (full FQDN with tag)
releaseImage: "quay.io/openshift-release-dev/ocp-release:4.17.11-multi"

etcd:
  storageClassName: lvms-vg1
  storageSize: 8Gi

# Scheduling of HCP control plane pods on hub
tolerations:
  - operator: Exists
    effect: NoSchedule
    key: node-role.kubernetes.io/master
nodeSelector:
  node-role.kubernetes.io/master: ""

# NodePool
nodePool:
  replicas: 2
  upgradeType: InPlace
  autoRepair: false
  nodeLabels: {}

# Cluster networking
networking:
  clusterCIDR: 172.26.0.0/16
  clusterHostPrefix: 21
  serviceCIDR: 172.25.64.0/18

# NodePort service publishing — ports must be unique per cluster on the same hub
services:
  oauthPort: ~
  konnectivityPort: ~
  ignitionPort: ~
  oidcPort: ~

logindConfig: |
  [Login]
  # SSH hardening
  StopIdleSessionSec=900
