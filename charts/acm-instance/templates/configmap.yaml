{{- if (include "apc-global-overrides.caCertificates" .) | fromYaml }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acm-instance.fullname" . }}-ca-certificates
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "acm-instance.labels" . | nindent 4 }}
data:
{{- include "apc-global-overrides.caCertificates" . | nindent 2 }}
{{- end }}
