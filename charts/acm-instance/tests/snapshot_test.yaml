suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: acm-instance
  namespace: open-cluster-management

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
