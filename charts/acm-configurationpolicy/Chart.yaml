apiVersion: v2
name: acm-configurationpolicy
description: A Helm chart to install ACM configurationpolicy CR using external tools, i.e. directly on managed clusters only (no placement decisions!)
type: application
version: 1.0.0
appVersion: "v1"
