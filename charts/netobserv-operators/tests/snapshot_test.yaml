suite: Snapshot test
templates:
  - charts/acm-operatorpolicy/templates/operatorpolicy.yaml
release:
  name: netobserv-operators
  namespace: openshift-netobserv-operator

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
