suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.lint.yaml
release:
  name: openshift-distributed-tracing

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
