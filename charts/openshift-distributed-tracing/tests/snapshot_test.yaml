suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.lint.yaml
release:
  name: kyverno-app-grafana

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
