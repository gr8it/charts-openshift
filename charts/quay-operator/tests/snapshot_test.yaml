suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: quay-operator
  namespace: quay-operator

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
