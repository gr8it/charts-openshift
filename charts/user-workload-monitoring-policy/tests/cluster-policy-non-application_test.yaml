suite: Rendering prometheus rules
templates:
  - cluster-policy-non-application.yaml
release:
  name: monitoring
  namespace: openshift-monitoring

tests:
  - it: rendered cluster-policy-cluster-non-application manifest
    asserts:
      - matchSnapshot: {}
