suite: Snapshot test
templates:
  - templates/*.yaml
values:
  - ../values.large.example.yaml
release:
  name: kafka
  namespace: kafka

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
