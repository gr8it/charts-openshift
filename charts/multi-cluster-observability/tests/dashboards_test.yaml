suite: Dashboard rendering
values:
  - ../values.lint.yaml
release:
  name: multi-cluster-observability
  namespace: open-cluster-management-observability

tests:
  - it: renders only the currently approved 19 dashboard ConfigMaps for hub
    template: templates/dashboards.yaml
    asserts:
      - hasDocuments:
          count: 19
      - matchSnapshot: {}

  - it: does not render dashboards for non-hub clusters
    template: templates/dashboards.yaml
    set:
      global.apc.cluster.isHub: false
    asserts:
      - hasDocuments:
          count: 0
