# ODF secondary networks merged into the cluster Network operator's
# additionalNetworks. Empty by default; set per cluster in the conf repo.
additionalNetworks: []
# - name: odf
#   namespace: openshift-storage
#   type: Raw
#   rawCNIConfig: |-
#     { "cniVersion": "0.3.1", "name": "odf", "type": "macvlan", ... }
