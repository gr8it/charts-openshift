suite: Snapshot test
templates:
  - templates/*
release:
  name: logging-operators
  namespace: openshift-logging

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
