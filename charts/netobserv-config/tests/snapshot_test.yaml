suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: netobserv-config
  namespace: netobserv

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
