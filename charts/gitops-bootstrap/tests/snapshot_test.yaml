suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: gitops-bootstrap
  namespace: apc-bootstrap

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
