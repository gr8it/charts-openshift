{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "kyverno-app-project values",
  "type": "object",
  "properties": {
    "projectMetadataConfigMapName": {
      "description": "Name of the ConfigMap an application team creates in its own namespace to declare project metadata.",
      "type": "string",
      "minLength": 1,
      "maxLength": 253,
      "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
    }
  }
}
