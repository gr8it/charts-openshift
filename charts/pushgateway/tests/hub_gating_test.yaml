suite: Hub gating
templates:
  - templates/serviceaccount.yaml
release:
  name: prometheus-pushgateway
  namespace: prometheus-pushgateway

tests:
  - it: skips wrapper resources on non-hub clusters
    template: templates/serviceaccount.yaml
    set:
      cluster.isHub: false
    asserts:
      - hasDocuments:
          count: 0
