suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: opentelemetry-operator
  namespace: apc-observability

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
