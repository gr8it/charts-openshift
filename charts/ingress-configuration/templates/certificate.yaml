apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-apps-{{ include "apc-global-overrides.clusterName" . }}-cert
  namespace: openshift-ingress
  labels:
    {{- include "ingress-configuration.labels" . | nindent 4 }}
spec:
  secretName: wildcard-apps-{{ include "apc-global-overrides.clusterName" . }}-cert
  issuerRef: 
    kind: ClusterIssuer
    name: {{ include "cert-manager-config.defaultClusterIssuer" . }}
  commonName: {{ include "cert-manager-config.ingressCertCommonName" . }}
  dnsNames:
  {{- range include "cert-manager-config.ingressCertDnsNames" . | fromJsonArray }}
    - "{{ . }}"
  {{- end }}