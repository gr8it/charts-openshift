
## uses following global values => do not set here
# global:
#   apc:
#     environmentShort: ~
## and their local overrides
## 1 character environment short name, e.g. 'p' for production
# environmentShort: ~ # if not specified, defaults to global.apc.environmentShort

argoEventsEventSource:
  serviceAccount:
    annotations: {}
argoEventsSensor:
  serviceAccount:
    annotations: {}
