suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: argo-events-project-setup
  namespace: apc-argo-project-setup

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
