apiVersion: v1
kind: Secret
metadata:
  name: {{ include "crossplane-vault-provider-config.fullname" . }}-approle
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "crossplane-vault-provider-config.labels" . | nindent 4 }}
stringData:
  credentials: |
    {{- required "Vault approle credentials are required" .Values.appRoleCredentials | nindent 4 }}
type: Opaque
