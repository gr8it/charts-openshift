{{- if .Values.objectBucketClaims.enabled }}
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: {{ tpl .Values.objectBucketClaims.restore.name . }}
  namespace: {{ .Values.sourceNamespace }}
  labels:
    {{- include "apc-global-overrides.labels" . | nindent 4 }}
    bucket-provisioner: openshift-storage.ceph.rook.io-bucket
spec:
  {{- $restoreLocation := (index .Values.dpa.backupLocations 1) }}
  bucketName: {{ tpl $restoreLocation.bucket . }}
  generateBucketName: {{ tpl .Values.objectBucketClaims.restore.name . }}
  objectBucketName: obc-{{ .Values.sourceNamespace }}-{{ tpl .Values.objectBucketClaims.restore.name . }}
  storageClassName: {{ .Values.objectBucketClaims.restore.storageClassName }}
{{- end }}