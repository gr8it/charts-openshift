suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: custom-metrics-autoscaler-config
  namespace: keda

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
