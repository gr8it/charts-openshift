global:
  compatibility:
    openshift:
      adaptSecurityContext: force

external-secrets:

  crds:
    annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true

  serviceMonitor:
    alwaysRender: true
    enabled: true

  webhook:
    metrics:
      service:
        enabled: true

  certController:
    metrics:
      service:
        enabled: true
