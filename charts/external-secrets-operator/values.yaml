global:
  compatibility:
    openshift:
      adaptSecurityContext: force

crds:
  annotations:
   argocd.argoproj.io/sync-options: ServerSideApply=true

serviceMonitor:
  enabled: true

webhook:
  metrics:
    service:
      enabled: true
certController:
  metrics:
    service:
      enabled: true