suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.lint.yaml
release:
  name: crossplane-keycloak-provider-bootstrap
  namespace: apc-crossplane-system

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
