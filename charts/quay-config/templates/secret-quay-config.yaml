---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "quay-config.name" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "quay-config.labels" . | nindent 4 }}
type: Opaque
stringData:
  config.yaml: |
    {{- tpl ($.Files.Get "files/config.yaml.tpl") $ | nindent 4 }}
