# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# uses following global values => do not set here
global:
  apc:
    cluster:
      environmentShort: ~

environmentShort: ~ # environment short name, e.g. D for dev, T for test, P for prod. If not set, .global.apc.cluster.environmentShort is used

gitopsNamespace: apc-gitops
adminRole: apc-project-admin
