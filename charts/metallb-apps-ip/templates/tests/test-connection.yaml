apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metallb-apps-ip.fullname" . }}-test-connection"
  labels:
    {{- include "metallb-apps-ip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metallb-apps-ip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
