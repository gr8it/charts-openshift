suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.lint.yaml
release:
  name: opentelemetry-collector

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
