suite: Snapshot test
templates:
  - templates/*
  - charts/*/templates/*
release:
  name: custom-metrics-autoscaler-operator
  namespace: custom-metrics-autoscaler-operator

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
