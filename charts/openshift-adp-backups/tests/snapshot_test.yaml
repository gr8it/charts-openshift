suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: openshift-adp-backups

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
