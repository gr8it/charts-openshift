suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: logging-helm
  namespace: openshift-logging

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
