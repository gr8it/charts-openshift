apiVersion: v2
name: identity-providers-config
description: Configuration Identity Provider in Standalone Openshift
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: apc-global-overrides
    version: "1.1.0"
    repository: https://raw.githubusercontent.com/gr8it/charts-openshift/refs/heads/main/