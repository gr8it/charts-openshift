suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: cluster-config-standalone
  namespace: openshift-config

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
