suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: cluster-monitoring
  namespace: openshift-monitoring

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
