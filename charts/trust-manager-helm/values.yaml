trust-manager:
  secretTargets:
    # some application consume ca root certificate bundles from secrets => enable, and allow management of secret ca-cert-bundle in all namespaces
    enabled: true
    authorizedSecrets:
      - ca-cert-bundle
