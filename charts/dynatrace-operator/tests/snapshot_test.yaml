suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: dynatrace-operator
  namespace: apc-dynatrace

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
