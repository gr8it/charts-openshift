suite: Snapshot test
templates:
  - templates/*
values:
  - ../values.example.yaml
release:
  name: crossplane-vault-provider
  namespace: apc-crossplane-system

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
