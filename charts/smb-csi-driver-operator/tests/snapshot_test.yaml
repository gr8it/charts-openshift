suite: Snapshot test
templates:
  - charts/*/templates/*
release:
  name: mb-csi-driver-operators
  namespace: openshift-cluster-csi-drivers

tests:
  - it: manifests should match snapshot
    asserts:
      - matchSnapshot: {}
