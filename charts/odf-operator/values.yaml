# OperatorPolicy Values
acm-operatorpolicy:
  subscription:
    channel: stable-4.18
    name: odf-operator
    namespace: openshift-storage
    source: redhat-operators
    sourceNamespace: openshift-marketplace
    startingCSV: odf-operator.v4.18.24-rhodf

  upgradeApproval: Automatic
  # Keep the currently-installed version in the allowed list as well.
  # Otherwise the OperatorPolicy reports non-compliant/failed during the upgrade.
  versions:
    - odf-operator.v4.17.5-rhodf
    - odf-operator.v4.18.24-rhodf
